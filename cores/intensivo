# === CONFIGURAÇÃO DE CORES DO TERMINAL ===
if [ "$TERM" != "dumb" ] && [ -z "$SSH_CONNECTION" ]; then
    # Apenas aplica se não estiver em SSH (opcional, remova se quiser em SSH também)
    printf '\033[44;37m'
    clear  # Limpa a tela para aplicar as cores uniformemente
fi